# 局域网验证反向测试文档

## 测试目的

验证局域网验证功能是否正确工作，确保：
1. 同一局域网内的设备可以正常签到
2. 不同局域网的设备被正确拒绝
3. 伪造代理头无法绕过验证
4. 配置修改后功能正常

---

## 前置条件

### 环境要求

- Docker 和 Docker Compose 已安装
- `jq` 命令行工具已安装（用于解析 JSON 响应）
- 项目已通过 `docker-compose up -d` 启动
- MySQL 已完成初始化（schema.sql + seed.sql 已执行）

### 环境检查

在执行任何测试前，先确认环境就绪：

```bash
# 1. 确认容器运行中
docker-compose ps
# 预期：checkin-app 和 checkin-mysql 状态为 Up

# 2. 确认应用可访问
curl -s http://localhost:3000/api/auth/student/login \
  -H "Content-Type: application/json" \
  -d '{"studentNo":"2021001","password":"password"}' | jq .code
# 预期输出：0

# 3. 确认当前网络配置
docker exec checkin-app cat /app/config/application.yml 2>/dev/null || \
  echo "请检查容器名称和配置路径"
```

### 当前配置状态

**文件**：`src/main/resources/application.yml`

```yaml
checkin:
  network:
    enable-forwarded-header: false
    trusted-proxies: []
    server-ip-override: "169.150.249.166"
    subnet-mask: "255.255.255.0"
```

**关键参数说明**：
- `server-ip-override`：手动指定服务器 IP，Docker 环境下必须设置为容器可达的 IP
- `subnet-mask`：/24 网段，同网段内最后一段可不同
- `enable-forwarded-header`：默认关闭，防止伪造 IP

### 测试数据说明

种子数据（`sql/seed.sql`）中包含：
- 教师 T001（id=1）拥有课程"Java程序设计"（id=1）
- 学生 2021001 ~ 2021005 已选修课程 1
- 签到活动需要由教师手动发起（测试前需确认有 OPEN 状态的 session）

---

## 测试方案

### 测试1：同网段签到（正向验证）

**目的**：确认当前配置下，同网段设备可以正常签到

**前置操作**：确保有一个 OPEN 状态的签到活动

```bash
# 步骤1：教师登录并发起签到活动
curl -s -c /tmp/teacher_cookies.txt -X POST http://localhost:3000/api/auth/teacher/login \
  -H "Content-Type: application/json" \
  -d '{"teacherNo":"T001","password":"password"}'

# 发起签到活动（课程1，持续30分钟）
SESSION_RESPONSE=$(curl -s -b /tmp/teacher_cookies.txt -X POST \
  "http://localhost:3000/api/teacher/course/1/sessions/open" \
  -H "Content-Type: application/json" \
  -d '{"durationMinutes":30}')

SESSION_ID=$(echo $SESSION_RESPONSE | jq -r '.data.id')
echo "签到活动ID: $SESSION_ID"

# 如果返回"已有进行中的签到活动"，先查询现有活动
# curl -s -b /tmp/teacher_cookies.txt "http://localhost:3000/api/teacher/sessions/history?courseId=1" | jq .
```

```bash
# 步骤2：学生登录
curl -s -c /tmp/student1_cookies.txt -X POST http://localhost:3000/api/auth/student/login \
  -H "Content-Type: application/json" \
  -d '{"studentNo":"2021001","password":"password"}'

# 验证登录成功
LOGIN_CODE=$(curl -s -c /tmp/student1_cookies.txt -X POST http://localhost:3000/api/auth/student/login \
  -H "Content-Type: application/json" \
  -d '{"studentNo":"2021001","password":"password"}' | jq -r '.code')

if [ "$LOGIN_CODE" != "0" ]; then
  echo "❌ 登录失败，终止测试"
  exit 1
fi
echo "✅ 登录成功"
```

```bash
# 步骤3：执行签到
SIGN_RESULT=$(curl -s -b /tmp/student1_cookies.txt -X POST \
  "http://localhost:3000/api/student/session/${SESSION_ID}/sign")

SIGN_CODE=$(echo $SIGN_RESULT | jq -r '.code')
echo "签到响应码: $SIGN_CODE"
echo "完整响应: $(echo $SIGN_RESULT | jq .)"
```

**预期结果**：
```json
{
  "code": 0,
  "message": "操作成功",
  "data": {
    "record": { "sessionId": ..., "clientIp": "169.150.249.166", ... },
    "message": "签到成功"
  }
}
```

**验证逻辑**：
```
客户端IP: 169.150.249.166（Docker 网络）
服务器IP: 169.150.249.166（server-ip-override 配置）
掩码:     255.255.255.0

(169.150.249.166 & 255.255.255.0) = 169.150.249.0
(169.150.249.166 & 255.255.255.0) = 169.150.249.0
169.150.249.0 == 169.150.249.0 → ✅ 同网段，允许签到
```

---

### 测试2：不同网段拒绝（反向验证）

**目的**：验证不同网段的设备被正确拒绝

**核心思路**：修改 `server-ip-override` 为一个与客户端不同网段的 IP，模拟服务器在另一个网段的场景。

> 注意：由于 Docker 环境下客户端 IP 固定，无法真正从不同网段发起请求。
> 此测试验证的是子网比较逻辑的正确性。

**步骤**：

```bash
# 步骤1：修改配置为不同网段
sed -i.bak 's/server-ip-override: "169.150.249.166"/server-ip-override: "192.168.1.100"/' \
  src/main/resources/application.yml

# 步骤2：重新构建并启动
docker-compose build app && docker-compose up -d

# 步骤3：等待应用就绪（轮询检测，而非硬编码 sleep）
echo "等待应用启动..."
for i in $(seq 1 30); do
  if curl -s http://localhost:3000/api/auth/student/login \
    -H "Content-Type: application/json" \
    -d '{"studentNo":"2021001","password":"password"}' 2>/dev/null | jq -r '.code' 2>/dev/null | grep -q "0"; then
    echo "✅ 应用已就绪（等待了 ${i} 秒）"
    break
  fi
  sleep 1
  if [ $i -eq 30 ]; then
    echo "❌ 应用启动超时，终止测试"
    mv src/main/resources/application.yml.bak src/main/resources/application.yml
    exit 1
  fi
done

# 步骤4：使用新学生登录（避免与测试1的签到记录冲突）
curl -s -c /tmp/student2_cookies.txt -X POST http://localhost:3000/api/auth/student/login \
  -H "Content-Type: application/json" \
  -d '{"studentNo":"2021002","password":"password"}'

# 步骤5：教师重新发起签到活动（重启后之前的 session 可能已过期）
curl -s -c /tmp/teacher_cookies2.txt -X POST http://localhost:3000/api/auth/teacher/login \
  -H "Content-Type: application/json" \
  -d '{"teacherNo":"T001","password":"password"}'

SESSION_RESPONSE2=$(curl -s -b /tmp/teacher_cookies2.txt -X POST \
  "http://localhost:3000/api/teacher/course/1/sessions/open" \
  -H "Content-Type: application/json" \
  -d '{"durationMinutes":30}')

SESSION_ID2=$(echo $SESSION_RESPONSE2 | jq -r '.data.id')
echo "新签到活动ID: $SESSION_ID2"

# 步骤6：尝试签到
SIGN_RESULT2=$(curl -s -b /tmp/student2_cookies.txt -X POST \
  "http://localhost:3000/api/student/session/${SESSION_ID2}/sign")

SIGN_CODE2=$(echo $SIGN_RESULT2 | jq -r '.code')
echo "签到响应码: $SIGN_CODE2"
echo "完整响应: $(echo $SIGN_RESULT2 | jq .)"

# 步骤7：验证日志
docker logs checkin-app 2>&1 | grep "非局域网访问" | tail -3

# 步骤8：恢复配置
mv src/main/resources/application.yml.bak src/main/resources/application.yml
docker-compose build app && docker-compose up -d
```

**预期结果**：
```json
{
  "code": 403,
  "message": "非局域网访问，禁止签到",
  "data": null
}
```

**验证逻辑**：
```
客户端IP: 169.150.249.166
服务器IP: 192.168.1.100（修改后的配置）
掩码:     255.255.255.0

(169.150.249.166 & 255.255.255.0) = 169.150.249.0
(192.168.1.100   & 255.255.255.0) = 192.168.1.0
169.150.249.0 != 192.168.1.0 → ❌ 不同网段，拒绝签到
```

---

### 测试3：X-Forwarded-For 伪造防护

**目的**：验证攻击者无法通过伪造 X-Forwarded-For 头绕过局域网验证

#### 场景A：未启用 forwarded-header（默认配置）

```bash
# 当前配置：enable-forwarded-header: false
# 即使携带伪造头，也会被忽略，直接使用 remoteAddr

curl -s -c /tmp/student3_cookies.txt -X POST http://localhost:3000/api/auth/student/login \
  -H "Content-Type: application/json" \
  -d '{"studentNo":"2021003","password":"password"}'

# 携带伪造的 X-Forwarded-For 头
SIGN_RESULT3=$(curl -s -b /tmp/student3_cookies.txt -X POST \
  -H "X-Forwarded-For: 10.0.0.50" \
  "http://localhost:3000/api/student/session/${SESSION_ID}/sign")

echo "响应: $(echo $SIGN_RESULT3 | jq .)"
```

**预期结果**：签到成功（code=0），因为 `enable-forwarded-header=false` 时 X-Forwarded-For 头被完全忽略，使用的是真实的 remoteAddr。

#### 场景B：启用 forwarded-header + 不可信代理

```bash
# 步骤1：修改配置
cat > /tmp/patch_forwarded.yml << 'EOF'
    enable-forwarded-header: true
    trusted-proxies: ["10.10.10.1"]
EOF

sed -i.bak \
  -e 's/enable-forwarded-header: false/enable-forwarded-header: true/' \
  -e 's/trusted-proxies: \[\]/trusted-proxies: ["10.10.10.1"]/' \
  src/main/resources/application.yml

# 步骤2：重新构建并等待就绪（同测试2的轮询方式）
docker-compose build app && docker-compose up -d
echo "等待应用启动..."
for i in $(seq 1 30); do
  if curl -s http://localhost:3000/api/auth/student/login \
    -H "Content-Type: application/json" \
    -d '{"studentNo":"2021001","password":"password"}' 2>/dev/null | jq -r '.code' 2>/dev/null | grep -q "0"; then
    echo "✅ 应用已就绪"
    break
  fi
  sleep 1
done

# 步骤3：登录
curl -s -c /tmp/student4_cookies.txt -X POST http://localhost:3000/api/auth/student/login \
  -H "Content-Type: application/json" \
  -d '{"studentNo":"2021004","password":"password"}'

# 步骤4：伪造 X-Forwarded-For（请求来源不在 trusted-proxies 中）
SIGN_RESULT4=$(curl -s -b /tmp/student4_cookies.txt -X POST \
  -H "X-Forwarded-For: 169.150.249.50" \
  "http://localhost:3000/api/student/session/${SESSION_ID}/sign")

echo "响应: $(echo $SIGN_RESULT4 | jq .)"

# 步骤5：检查日志中的警告
docker logs checkin-app 2>&1 | grep "不可信代理" | tail -3

# 步骤6：恢复配置
mv src/main/resources/application.yml.bak src/main/resources/application.yml
docker-compose build app && docker-compose up -d
```

**预期结果**：签到结果取决于 remoteAddr 是否与服务器同网段。关键验证点是日志中出现 `X-Forwarded-For 头来自不可信代理` 警告，说明伪造的头被正确识别并丢弃。

**代码逻辑参考**（[IPUtil.java:61-66](src/main/java/com/checkin/util/IPUtil.java#L61-L66)）：
```java
String remoteAddr = request.getRemoteAddr();
if (ip != null && !trustedProxies.isEmpty() && !trustedProxies.contains(remoteAddr)) {
    logger.warn("X-Forwarded-For 头来自不可信代理: {}", remoteAddr);
    ip = null; // 丢弃伪造的 IP，回退到 remoteAddr
}
```

---

### 测试4：自动 IP 识别功能

**目的**：验证 `server-ip-override` 留空时，系统能自动识别服务器 IP

```bash
# 步骤1：清空 server-ip-override
sed -i.bak 's/server-ip-override: "169.150.249.166"/server-ip-override: ""/' \
  src/main/resources/application.yml

# 步骤2：重新构建并等待就绪
docker-compose build app && docker-compose up -d
echo "等待应用启动..."
for i in $(seq 1 30); do
  if curl -s http://localhost:3000/api/auth/student/login \
    -H "Content-Type: application/json" \
    -d '{"studentNo":"2021001","password":"password"}' 2>/dev/null | jq -r '.code' 2>/dev/null | grep -q "0"; then
    echo "✅ 应用已就绪"
    break
  fi
  sleep 1
done

# 步骤3：查看自动识别的 IP
docker logs checkin-app 2>&1 | grep "自动识别服务器 IP" | tail -1
# 预期输出类似：INFO com.checkin.util.IPUtil - 自动识别服务器 IP: 172.20.0.3

# 步骤4：尝试签到（预期失败，因为容器内网 IP 与客户端 IP 不同网段）
curl -s -c /tmp/student5_cookies.txt -X POST http://localhost:3000/api/auth/student/login \
  -H "Content-Type: application/json" \
  -d '{"studentNo":"2021005","password":"password"}'

SIGN_RESULT5=$(curl -s -b /tmp/student5_cookies.txt -X POST \
  "http://localhost:3000/api/student/session/${SESSION_ID}/sign")

echo "响应: $(echo $SIGN_RESULT5 | jq .)"

# 步骤5：恢复配置
mv src/main/resources/application.yml.bak src/main/resources/application.yml
docker-compose build app && docker-compose up -d
```

**预期结果**：
```json
{
  "code": 403,
  "message": "非局域网访问，禁止签到",
  "data": null
}
```

**原因**：自动识别的服务器 IP 是 Docker 容器内网 IP（如 172.20.0.3），与客户端 IP（169.150.249.166）不在同一 /24 网段。

---

### 测试5：IP 格式校验

**目的**：验证非法 IP 格式被正确拒绝

此测试验证 `IPUtil.isValidIPv4()` 的正则校验逻辑（[IPUtil.java:23-25](src/main/java/com/checkin/util/IPUtil.java#L23-L25)），通过单元测试覆盖，见 `src/test/java/com/checkin/util/IPUtilTest.java`。

**运行单元测试**（通过 Docker，无需本地 JDK）：
```bash
docker run --rm -v "$(pwd)":/app -w /app maven:3.9-eclipse-temurin-17 \
  mvn clean test -Dtest=com.checkin.util.IPUtilTest -Dsurefire.useFile=false
```

**预期输出**：
```
Tests run: 14, Failures: 0, Errors: 0, Skipped: 0
BUILD SUCCESS
```

**测试覆盖内容**（14 个用例）：
- `isValidIPv4`：标准地址、边界值、超出范围、格式错误、空值/null
- `isInSameSubnet`：同网段/不同网段（/24、/16、/30 掩码）、相同 IP、非法 IP
- `getServerIP`：手动配置优先、空配置自动识别、缓存生效

**校验规则**：
| 输入 | 预期结果 | 说明 |
|------|----------|------|
| `192.168.1.1` | ✅ 有效 | 标准 IPv4 |
| `0.0.0.0` | ✅ 有效 | 全零地址 |
| `255.255.255.255` | ✅ 有效 | 广播地址 |
| `256.1.1.1` | ❌ 无效 | 超出范围 |
| `192.168.1` | ❌ 无效 | 缺少段 |
| `192.168.1.1.1` | ❌ 无效 | 多余段 |
| `abc.def.ghi.jkl` | ❌ 无效 | 非数字 |
| `null` | ❌ 无效 | 空值 |
| `""` | ❌ 无效 | 空字符串 |

---

## 测试检查清单

### 功能测试
- [ ] 同一网段设备可以签到（测试1）
- [ ] 不同网段设备被拒绝，返回 403（测试2）
- [ ] 配置修改后重新构建功能正常（测试2）
- [ ] 自动 IP 识别功能正常（测试4）
- [ ] IP 格式校验正常（测试5 / 单元测试）

### 安全测试
- [ ] 默认配置下 X-Forwarded-For 头被忽略（测试3A）
- [ ] 不可信代理的 X-Forwarded-For 被丢弃并记录警告（测试3B）

### 配置测试
- [ ] `server-ip-override` 配置生效
- [ ] `subnet-mask` 配置生效
- [ ] `enable-forwarded-header` 配置生效
- [ ] `trusted-proxies` 配置生效

### 日志测试
- [ ] 局域网验证失败时记录 WARN 日志，包含 clientIp 和 serverIp
- [ ] 自动识别 IP 时记录 INFO 日志
- [ ] 不可信代理时记录 WARN 警告

---

## 常见问题排查

### 问题1：配置修改后不生效

**原因**：Docker 镜像未重新构建，配置打包在镜像中

**解决**：
```bash
docker-compose build app && docker-compose up -d
```

### 问题2：签到返回 409 而非 403

**原因**：该学生已在此 session 中签到过（重复签到）

**解决**：使用不同的学生账号，或发起新的签到活动：
```bash
# 发起新签到活动
curl -b /tmp/teacher_cookies.txt -X POST \
  "http://localhost:3000/api/teacher/course/1/sessions/open" \
  -H "Content-Type: application/json" \
  -d '{"durationMinutes":30}'
```

### 问题3：签到返回"签到活动不存在"或"签到活动已关闭"

**原因**：session 已过期或被关闭

**解决**：重新发起签到活动，确认 session 状态为 OPEN

### 问题4：签到返回"您未选修此课程"

**原因**：测试学生未选修对应课程

**解决**：确认使用的学生已选修该课程。种子数据中学生 2021001~2021005 已选修课程 1。

### 问题5：Docker 环境下所有请求 IP 相同

**原因**：Docker 网络模式导致所有请求经过同一网关

**解决**：这是 Docker 环境的正常行为。生产环境部署时，不同设备会有不同的真实 IP。开发环境下通过修改 `server-ip-override` 来模拟不同网段场景。

### 问题6：日志中看不到 IP 相关信息

**原因**：日志级别过高

**解决**：
```yaml
logging:
  level:
    com.checkin: DEBUG
```

---

## 生产环境部署建议

### 配置示例

**场景A：单一局域网（教室场景）**
```yaml
checkin:
  network:
    server-ip-override: ""          # 自动识别服务器 IP
    subnet-mask: "255.255.255.0"    # /24 网段
```

**场景B：多子网校园网**
```yaml
checkin:
  network:
    server-ip-override: "10.1.0.100"
    subnet-mask: "255.255.0.0"      # /16 网段，覆盖更大范围
```

**场景C：使用 Nginx 反向代理**
```yaml
checkin:
  network:
    enable-forwarded-header: true
    trusted-proxies: ["192.168.1.1"]  # Nginx 服务器 IP
    server-ip-override: "192.168.1.100"
    subnet-mask: "255.255.255.0"
```

Nginx 配置需添加：
```nginx
proxy_set_header X-Forwarded-For $remote_addr;
```

### 部署前检查

```bash
# 检查服务器 IP
ip addr show | grep "inet " | grep -v 127.0.0.1

# 检查子网掩码
ip addr show | grep "inet " | grep -v 127.0.0.1 | awk '{print $2}'

# 检查路由
ip route show
```

---

**文档版本**: v2.0
**最后更新**: 2026-02-13
